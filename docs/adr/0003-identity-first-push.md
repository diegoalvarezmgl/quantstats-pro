# Identity-only first push to GitHub

The first push contains upstream code plus fork identity (README, pyproject.toml, version 0.1.0, CHANGELOG, CONTRIBUTORS, CI, ADRs). No bugfixes bundled — each fix gets its own commit afterward for a clean portfolio history.

**Considered:** Include bugfixes in first push — rejected because it blurs setup vs. substantive work in git history.
